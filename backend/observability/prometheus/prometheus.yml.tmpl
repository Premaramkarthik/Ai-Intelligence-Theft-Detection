global:
  scrape_interval: 5s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: rtsp_camera_backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - __PROMETHEUS_SCRAPE_TARGET__
