apiVersion: 1

providers:
  - name: pipeline-backend
    orgId: 1
    folder: Pipeline Backend
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /etc/grafana/dashboards
